/**
 * MUSA — runtime config of THIS deployment, loaded before assets/js/api.js.
 *
 * This file is the platform demo's stub (in-browser mock data). Client builds
 * overwrite it: the builder emits data/runtime.js pointing the frontend at the
 * static collection API under api/ (mode "live" + static — see ADR 0008 and
 * docs/api-estatica.md).
 */
window.MUSA_RUNTIME = { mode: "mock" };
